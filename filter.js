// filter all the odd number s from an array
const allNumbers = [2, 4, 6, 8, 9, 11, 13, 15];
// const callBack = (x) => console.log(x);
const output = allNumbers.filter((x) => x % 2 == 1);
console.log(output);
