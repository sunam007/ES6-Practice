// simple one liner arrow function
const devideBy5 = (x) => x / 5;
console.log(devideBy5(100));
// simple multiline arrow function
const doMath = (x, y) => (x + 2) * (y + 2);

console.log(doMath(3, 5));
